<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.transaction.notifymsg.account.mapper.AccountInfoMapper">

    <update id="updateAccoutBalanceByAccountNo">
       update account_info set account_balance = account_balance + #{payBalance} where account_no = #{accountNo}
    </update>

</mapper>